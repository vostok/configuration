namespace Vostok.Configuration.Extensions
{
    // CR(krait): Should be moved to .Binders or to the root namespace.
    internal enum BinderAttribute
    {
        IsRequired = 1,
        IsOptional = 2,
    }
}